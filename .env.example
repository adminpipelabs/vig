# Vig - Rolling Bet Bot Config

# Wallet private key (Polygon, must be onboarded on Polymarket)
PRIVATE_KEY=0x...

# Polygon RPC — free options: https://polygon-rpc.com or get one from Alchemy/Infura
RPC_URL=https://polygon-rpc.com

# Bot strategy
MAX_BETS=10           # max simultaneous positions
BET_SIZE=10           # USDC per bet
MIN_PRICE=0.60        # min YES price to enter
MAX_PRICE=0.80        # max YES price to enter
EXPIRY_WINDOW=60      # only bet on markets expiring within this many minutes
POLL_SECONDS=60       # how often the bot runs its loop
